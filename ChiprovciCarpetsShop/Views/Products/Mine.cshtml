@model IEnumerable<ProductServiceModel>

@{ 
    ViewBag.Title = "My Products";
    ViewBag.AllowEditing = true;
} 

@if (Model.Any())
{
    <h2 class="text-center">You have no products added!</h2>
}

<partial name="_ProductsPartial" model="@Model" />
